{
  "routes": [
    {
      "method": "GET",
      "path": "/working-times",
      "handler": "working-time.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/working-times/count",
      "handler": "working-time.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/working-times/:id",
      "handler": "working-time.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/working-times",
      "handler": "working-time.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/working-times/:id",
      "handler": "working-time.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/working-times/:id",
      "handler": "working-time.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
